use crate::erros;
use crate::itens;

/*
pub trait Quardador {
    fn quardar<Im: itens::Item>(&mut self, im: Im) -> Result<(), (Im, erros::itens::Erro)>;
}
*/

/*
pub trait Estocador {
    fn estocar<Gs: Gas>(&mut self, gs: Gs) -> Result<(), (Gs, gases::erro)>;
}
*/
